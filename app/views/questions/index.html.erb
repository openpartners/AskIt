<h1>Questions</h1>

<% @questions.each do |question| %>
	<p><%= question.id %></p>
	<time datetime="<%= question.formatted_created_at%>">
		<%= question.formatted_created_at %>
	</time>
	<p><%= question.title %></p>
	<p><%= question.body %></p>
<% end %>
